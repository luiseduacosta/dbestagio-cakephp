<?php if ($id) {; ?>
	<h1>Supervisores da Instituição: <?php echo $supervisores[0]['Instituicao']['instituicao']; ?></h1>
<?php } else {; ?>
	<h1>Supervisores</h1>
<?php }; ?>

<?php 

// pr($supervisores);

?>

<?php 

if ($id) {

	foreach ($supervisores[0]['Supervisor'] as $c_supervisor) {
		echo ($c_supervisor['id']) . " " . ($c_supervisor['nome']) . "<br>";
		}
} else {

	foreach ($supervisores as $c_instituicao) {

	// pr($c_instituicao);
	// pr($c_instituicao['Instituicao']['instituicao']);
		foreach ($c_instituicao['Supervisor'] as $cada_supervisor) {

			// pr($cada_supervisor['nome']);

			echo $cada_supervisor['nome'] . " " . $c_instituicao['Instituicao']['instituicao'] . "<br />";
		}
	}

}

?>
